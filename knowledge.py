"""狼人杀「常识速通」手册：喂给 NPC 的 LLM 提示，让它们真正懂规则、会用黑话、看票型。

注意（已按本仓库实际板子适配）：
- 本游戏当前板子只有 **狼人 / 预言家 / 女巫 / 平民**，没有猎人、守卫、警长、警徽流。
  所以下面**特意标注**了哪些术语在本局不适用，避免 NPC 张口就提不存在的角色/机制。
- `WEREWOLF_PLAYBOOK` 是注入版（精简、贴合本局），由 npc.py 拼进发言提示。
- `COMMON_SENSE_FULL` 是完整版常识，仅作参考/文档，不注入（保留猎人/守卫等通识）。
"""
from __future__ import annotations

from game.roles import Role

# ============================================================
# 注入版：拼进 NPC 发言提示，让发言有狼人杀「常识感」
# 改为按「本局实际板子」动态生成：有猎人/守卫就讲它们的规则，没有就提醒别提。
# ============================================================
_PLAYBOOK_HEAD = """【狼人杀常识（本局适用）】
· 胜负看「屠边」：好人靠白天投票放逐所有狼获胜；狼杀光所有神职或杀光所有平民即赢。
  所以狼通常优先点神（预言家、女巫等）。"""

# 各神职的要点提示（仅在本局存在该角色时才注入）
_ROLE_TIPS: dict[Role, str] = {
    Role.SEER: "· 预言家：每晚验一人，得「金水」（好人）或「查杀」（狼），核心是带队抓狼。",
    Role.WITCH: "· 女巫：一瓶解药一瓶毒药（各一次），同一晚一般不既救又毒；狼可能自刀骗解药。",
    Role.HUNTER: "· 猎人：出局（被狼刀或被票出）时能开枪带走一人，但被女巫毒死则不能开枪；"
                 "可适时亮身份威慑狼。",
    Role.GUARD: "· 守卫：每晚守护一人免遭狼刀，不能连守同一人；注意『同守同救』"
                "（守卫和女巫同一夜救同一人）那人照样会死，要算好。",
    Role.IDIOT: "· 白痴：被投票放逐时自动翻牌免死一次，但翻牌后永久失去投票权；"
                "白痴仍可发言分析，但不能再投票。",
    Role.KNIGHT: "· 骑士：白天发言时可亮牌与一名玩家翻牌决斗——对方是狼则狼死，"
                 "对方不是狼则骑士自己死，一局只能用一次。",
    Role.WOLF_KING: "· 白狼王：狼人阵营，和狼队友一起刀人；被投票放逐出局时可带走一名玩家"
                    "（类似猎人的开枪，但是狼方的）。",
}

_PLAYBOOK_TAIL = """· 黑话：悍跳=狼冒充预言家；倒钩=狼表面上站真预言家、实则搅水坑队友；冲锋=狼死命站边
  狼队友；金水=被验的好人，查杀=被验的狼；银水=被女巫救起的人（大概率好人但不绝对）；
  扛推=没抓到狼时先推一个最可疑的；归票=投票时集中火力投某个人；表水=自证清白（清楚
  复述自己夜晚/白天做了什么，逻辑要闭环、别前后矛盾）。
· 打法常识：第一天要「站边」（认一个你信的预言家）；看票型往往比听发言更真——倒钩狼
  常去投真预言家、冲锋狼会投悍跳狼，弃票过多的人也容易有问题；被怀疑时清晰表水、把逻辑说圆。
· 轮次计算：狼刀在先。比如剩 1 神 1 民 1 狼，夜里狼刀神、白天狼再跟票，好人就输了；
  所以救不救、守谁、白天投谁，都要算好还剩几轮。
· 大忌——贴脸/场外：用发誓、人身攻击，或「我根本没睁眼」这类游戏外信息来自证，
  既没说服力又破坏体验，绝对不要这么干。"""


def playbook_for(roles_present) -> str:
    """按本局实际出现的角色拼出常识手册：讲清在场神职，提醒别提不存在的机制。"""
    present = set(roles_present)
    order = [Role.WEREWOLF, Role.WOLF_KING, Role.SEER, Role.WITCH,
             Role.HUNTER, Role.GUARD, Role.IDIOT, Role.KNIGHT, Role.VILLAGER]
    names = [r.cn for r in order if r in present]
    board_line = "· 本局板子有【" + " / ".join(names) + "】。"

    absent = []
    for r, name in [(Role.HUNTER, "猎人"), (Role.GUARD, "守卫"),
                    (Role.IDIOT, "白痴"), (Role.KNIGHT, "骑士"),
                    (Role.WOLF_KING, "白狼王")]:
        if r not in present:
            absent.append(name)
    absent.append("警长/警徽流")
    board_line += "没有" + "、".join(absent) + "——发言里别提这些不存在的角色或机制。"

    tips = [board_line]
    tips.append("· 狼人：夜里刀人、白天伪装好人，可自刀骗女巫解药。")
    for r in order:
        if r in present and r in _ROLE_TIPS:
            tips.append(_ROLE_TIPS[r])
    if Role.VILLAGER in present:
        tips.append("· 平民：无技能，靠逻辑和票型找狼。")

    return "\n".join([_PLAYBOOK_HEAD, *tips, _PLAYBOOK_TAIL])


# 默认（auto 板：狼/预/女/民）注入文本，向后兼容旧的引用方式。
WEREWOLF_PLAYBOOK = playbook_for(
    {Role.WEREWOLF, Role.SEER, Role.WITCH, Role.VILLAGER})


# ============================================================
# 完整版：原始「常识速通」的整理稿，仅供参考（不注入提示）。
# 含本局暂未实现的猎人/守卫/警长等通识，便于日后扩展板子时取用。
# ============================================================
COMMON_SENSE_FULL = """狼人杀常识速通

一、胜负核心（记住「屠边」）
不杀光所有人，只分两个阵营：
· 好人阵营（神 + 民）：白天投票放逐所有狼人。
· 狼人阵营：「屠边」——杀光所有神职（预言家、女巫等）或杀光所有平民即获胜。

二、常见角色（以「预女猎守」板为例；本仓库当前只实现了狼/预/女/民）
· 狼人：夜间睁眼刀人，白天伪装好人，可「自刀」骗女巫药。
· 预言家：每晚验一人，得「金水」（好人）或「查杀」（狼）；核心是拿警徽、带队。
· 女巫：一瓶解药（救夜间被刀者）+ 一瓶毒药（毒一人）。注意同守同救会死。
· 猎人：出局可开枪带走一人（被毒则不能开枪）。
· 守卫：夜间守护一人免遭狼刀，不能连续两晚守同一人。

三、黑话词典
· 警徽流：预言家的警徽移交计划，验好人传警徽、验狼给前夜金水或撕警徽，以此传验人信息。
· 悍跳 / 冲锋 / 倒钩：狼冒充预言家叫悍跳；死站边狼队友叫冲锋；表面站真预言家、实则
  出卖队友叫倒钩。
· 银水：女巫夜里救起的人（大概率好人但不绝对）。
· 金水 / 查杀：验出的好人 / 狼人。
· 扛推 / 归票：找不到狼时推个疑似狼的叫扛推；最后由警长总结、指定放逐谁叫归票。
· 贴脸 / 场外：大忌！用发誓、人身攻击或场外信息自证，破坏体验，正规局直接判负。

四、发言与逻辑
· 站边：第一天必须认一个预言家，逻辑基点是谁的警徽流合理、谁视角里狼坑完整。
· 看票型：比发言更真。倒钩狼会投真预言家，冲锋狼会投悍跳狼，弃票过多往往出问题。
· 表水：被怀疑时清晰复述自己夜晚和白天做了什么，逻辑要闭环、别前后矛盾。
· 轮次计算：狼刀在先。剩 1 神 1 民 1 狼，夜里狼刀神、白天狼绑票，好人就输；
  所以守卫和女巫要算好轮次决定救不救。"""
